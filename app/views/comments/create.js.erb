$('.<%= @comment.commentable.class %>-comments-count-<%= @comment.commentable.id %>')
  .html('<%= @comment.commentable.comments.count unless @comment.parent_id?%><%= @comment.commentable.replies.count if @comment.parent_id?%>')

$('.<%= @comment.commentable.class %>-comments-<%= @comment.commentable.id %>')
  .html('<%= j render @comment.commentable.comments unless @comment.parent_id?%><%= j render @comment.commentable.replies if @comment.parent_id?%>')

$('.<%= @comment.commentable.class %>-comments-form-<%= @comment.commentable.id %>')
  .html('<%= j render 'comments/form', object: @comment.commentable, type: @comment.commentable.class, parent_id: nil, path_type: @comment.commentable.comments.build  unless @comment.parent_id?%><%= j render 'comments/form', object: @comment.commentable, type: @comment.commentable.class, parent_id: @comment.parent_id, path_type: @comment.commentable.replies.build if @comment.parent_id?%>')
